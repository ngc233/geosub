# 2.10.1：Claude Pro 计费说明

2026-09-07 已推送并部署，版本 2.10.1，提交 `f47ca49fdccd5d62bcd2dc72160d854bd793f2b1`，标签 `v2.10.1`。

解决 Claude Pro App Store 月付与官网年付折算容易混淆的问题，并明确订阅与 API 费用区别。仅修改中英文 Claude Pro 计费说明及两条 FAQ，正文和 JSON-LD 同源，附官方依据。经用户批准，sitemap 产品与套餐分类预算校准为 98，总预算保持 148。

## 验证结果

- 本地完整发布检查与数据库预检通过；623 项前端测试、构建和依赖审计通过。
- [GitHub CI](https://github.com/ngc233/geosub/actions/runs/34093200661) 成功。发布文件树与本地已测试候选一致。
- 标準升级脚本完成备份校验、构建、迁移门禁和健康检查。没有执行内容回填。
- 中英文 Claude Pro 页面 HTTP 200，FAQ 由 7 条增至 9 条；元数据保持一致。两个冻结 ChatGPT 页面元数据与 JSON-LD 未变。
- sitemap 144 条 URL 均直达 HTTP 200。Chrome 验证新说明、来源链接及 API FAQ 展开。

## 待完成

后台浏览器登录失效，采集工作台的 AI/流媒体抽样及后台版本文字尚未验证。既有 Bing shadow 采集失败和一条过期价格仍需独立跟进；网站健康检查通过不等于所有采集服务正常。

IndexNow 未提交，增长效果尚未验证。分别观察中英文两页的搜索曝光、点击与 CTR，确认重新抓取并取得完整结算窗口后再评估，避免将短期波动归因于文案。AI API 开发继续暂停。

详细部署与回退证据仅保存在本机私有运维记录中。前一生产版本为 2.10.0；需要回退时遵循标准 RELEASE.md 流程。
