# 2.10.1 发布候选：Claude Pro 计费说明

2026-09-07：仅本地准备完成，未推送、打标签或部署。候选版本 2.10.1；目前线上版本仍为 2.10.0。

## 用户问题与范围

帮助阅读 Claude Pro 地区价格表的用户区分 App Store 月付、官网年付折算价和独立 API 费用。中文、英文详情页增加来源可查的说明与两条 FAQ；可见 FAQ 和 JSON-LD 同源。内容提交 `d0c3be7`。

用户另行批准产品概览＋套餐 sitemap 分类预算由 96 校准为 98，总预算保持 148，政策提交 `695d32a`。没有新增或删除 URL，没有扩大语言或推广名单。

标题、描述、H1、canonical、robots、价格口径和两个冻结 ChatGPT 实验保持不变。AI API 分支不在本次候选范围内。没有新增 schema、生产数据迁移或回填；部署时保持 `GEOSUB_RUN_BACKFILLS=false`。

## 已验证

- 远端 main 已重新获取，仍为 `9917775d19c1285fa331731cba0daa4409f05d3c`；候选以该提交为基线。
- 标准 `release-check.ps1 -BaselineRef origin/main` 完整通过，包括版本递增、卫生与敏感信息检查、迁移清单、前后端依赖审计（均 0 漏洞）、SEO、类型、Lint、623 项前端测试及生产构建。
- 隔离测试数据库的 `preflight:full` 完整通过，sitemap 分类 98/98、总量 144/148。测试库原先落后于现有代码：仅补齐已有 056 函数定义，并通过标准汇率脚本采集 36/36 条真实汇率后复测。旧函数定义已保存在工作区私有证据目录，未执行价格隔离函数或生产 SQL。
- 内容页面四态及 Chrome 代表页检查、FAQ 键盘操作和 HTML/JSON-LD 对照见 `CLAUDE_PRO_BILLING_SEO.md`。浅色使用本地 CSS 测试预览，未切换操作系统主题。
- SSH 已恢复；生产仓库提交 `b5028bc153e2c6cf74e2a4dec4b3c98c90fed44a`、版本 2.10.0。公网健康接口返回 process/database/catalog/exchangeRates 均正常，35 个已发布产品、2666 条正式价格、62 条有效汇率（此次读取快照，不写入页面）。
- 经服务器读取的公网 sitemap 144 条路径与候选基线完全一致。两个 Claude Pro 页面和两个冻结 ChatGPT 页面均 HTTP 200、index/follow，元数据及 JSON-LD 已保存为发布前基线。Python 默认 HTTP 客户端曾遇到 403；通过读取 sitemap/健康接口同样使用的 curl 客户端完成，没有更改访问控制。

证据保存在工作区 `work/private/release-2.10.1/`。本地价格质量检查仍报告部分产品待审核异常比例偏高的既有警告；相关记录未被提升为正式价格，不属于本次修订。

## 发布执行与观察

下一步需用户明确授权本次 2.10.1 推送和部署，遵循 `RELEASE.md`。推送后核对远端 SHA 和 CI；正式升级使用标准 upgrade.sh，在停服前校验固定提交、完成备份和迁移门禁，禁止跳过检查。发布成功后才写实际部署日期与发布记录，不提前把 Unreleased 改为已发布。

部署后验证健康、版本显示、目标页与冻结页元数据、FAQ、sitemap、定时器和后台采集状态。标准备份、远端 CI、实际部署和部署后验收尚未执行，候选检查通过不代表已经上线。

内容观察以实际发布时间为起点，Google/Bing 分开统计两页曝光、点击、CTR 与官方说明点击；确认重新抓取并取得完整结算窗口后再判断，不将短期波动归因于文案。IndexNow 需发布后另按授权执行。

回退基线为现有 2.10.0 的已成功部署记录；如发布失败，按标准 rollback.sh 的部署 ID 和确认令牌执行应用回退，不自动恢复数据库。实际回退 ID 需在发布窗口读取确认。
