# 2.10.2 发布候选：X Premium 内容与索引待审核

2026-09-08：本地候选，未推送、打标签或部署。基线为远端 main `a71884e`，其生产运行时代码为已发布的 2.10.1 / `f47ca49`。内容提交为 `dfda9cf`。

## 用户问题与行为

X Premium 资料不足以帮助用户区分三档权益与购买渠道。补充中英文三档说明及四项 FAQ，解释蓝标审核、赞助内容例外、关联 X 账号的 Grok 权益和取消渠道。官方来源复核日期与价格采集日期分开，保持 App Store 月付范围。

内容质量不再自动放行该产品的索引：X Premium 概览与套餐显式待审核，保持 noindex/follow，推广名单和 observe 模式不能绕过。质量分数仍按实际内容计算。完整实现及来源见 [X_PREMIUM_CONTENT_HOLD.md](X_PREMIUM_CONTENT_HOLD.md)。

## 范围与发布边界

版本文件、前后端包及锁文件同步至 2.10.2，部署后后台版本标识据此更新。总 sitemap 预算 148、产品和套餐预算 98 不变。没有新增 URL、语言、推广项、依赖、schema、数据库迁移或回填。普通部署保持 `GEOSUB_RUN_BACKFILLS=false`。

冻结 ChatGPT 实验及 Claude Pro 内容保持不变。X Premium 标题摘要和结构化描述可随本次真实编辑内容更新，但 URL、价格范围和 noindex 状态不变。本次不提交 IndexNow。

## 验证记录

本地实现已有 625 测试、lint、typecheck、构建、SEO、内容重复度、sitemap 与代表性四态 UI 验证。8 个中英文 X Premium 页面 robots 符合预期；sitemap 144 条路径与基线相同；冻结页和 Claude Pro 的元数据、JSON-LD 一致。深色 UI 使用本地 CSS fixture 模拟，未改变系统主题。

2.10.2 标准 `release-check.ps1 -BaselineRef origin/main` 通过，验证 2.10.1 → 2.10.2 递增，前后端依赖审计均为 0 漏洞，625 项前端测试及生产构建通过。隔离本地库 `preflight:full` 通过，包含迁移清单、价格、套餐、Logo、国家页和 sitemap 检查。首次数据库预检因汇率超过 18 小时失败；通过现有同步脚本真实更新本地测试库 36/36 汇率后完整复测通过，未修改生产库。既有待审核价格异常比例警告未被提升为正式价格。

远端 CI、生产备份与部署后验证尚未执行。

## 发布与回退

按 RELEASE.md 获得本次推送和部署明确授权后，推送固定候选并核对 SHA、CI；再使用标准 upgrade.sh，执行备份、版本递增与健康门禁。发布后检查后台版本、健康、目标页及冻结页、sitemap 和采集工作台。

已知上个发布记录中后台浏览器抽样未完成、Bing shadow 失败及过期价格需独立复核，不据此声称采集全部健康。生产状态必须在实际发布窗口重新读取。

整体应用回退目标是上次成功的 2.10.1，通过标准 rollback.sh 并核对部署 ID；不自动恢复数据库。若仅撤销内容，应保留显式索引待审核，不能单独移除 hold 而留下达标内容。没有数据迁移需要撤销。

## 结果评估

解决的问题是套餐和渠道认知不足。上线后才开始记录目标页浏览、官方来源点击及反馈；现有两天 eligible PV 样本不能证明增长效果。索引保持关闭，因此不以此次内容发布承诺新增自然搜索流量。
